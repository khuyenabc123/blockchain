\subsection{Back-end API}

    The Web2 off-chain engine is driven by a structured Node.js/Express REST API cluster. It serves as a stateless coordinator layer that translates incoming client requests into secure database and blockchain state updates.

    \input{tables/be_apis}
